# Willa's AI Training 101

An OBSOLETE training experience for the Willa's team.

## Stack
- Static site (single `index.html`), deployed on Vercel
- Auto-deploys from GitHub `main` on every push

## Local
Open `index.html` in a browser. No build step.

## Deploy
Push to `main` → Vercel rebuilds automatically.
